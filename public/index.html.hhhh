<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>StudyHub 2.0 - Elite Learning</title>
    
    <!-- Biblioteca para o Grafo de Conhecimento 3D -->
    <script type="text/javascript" src="https://unpkg.com/vis-network/standalone/umd/vis-network.min.js"></script>

    <style>
        :root {
            --bg-primary: #ffffff;
            --bg-secondary: #f8f9fa;
            --bg-card: #ffffff;
            --text-primary: #1a1a1a;
            --text-secondary: #6c757d;
            --border: #e0e0e0;
            --accent: #6366f1;
            --accent-hover: #4f46e5;
            --success: #10b981;
            --warning: #f59e0b;
            --danger: #ef4444;
            --shadow: rgba(0, 0, 0, 0.1);
        }

        [data-theme="dark"] {
            --bg-primary: #1a1a1a;
            --bg-secondary: #2d2d2d;
            --bg-card: #242424;
            --text-primary: #ffffff;
            --text-secondary: #a0a0a0;
            --border: #404040;
            --shadow: rgba(0, 0, 0, 0.3);
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--bg-primary); color: var(--text-primary); transition: all 0.3s; line-height: 1.6; }

        /* ========== CONTROLE DE ACESSO (PAIS) ========== */
        body.is-parent .hide-on-parent { display: none !important; }
        .parent-dashboard { display: none; background: linear-gradient(135deg, var(--warning), #d97706); color: white; padding: 15px 20px; text-align: center; }
        body.is-parent .parent-dashboard { display: block; }
        .student-inbox { display: none; background: rgba(99, 102, 241, 0.1); border-left: 4px solid var(--accent); padding: 15px; margin-bottom: 20px; border-radius: 8px; }
        body.is-student .student-inbox { display: block; }

        /* ========== LOGIN ========== */
        .login-container { min-height: 100vh; display: flex; align-items: center; justify-content: center; background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%); padding: 20px; }
        .login-box { background: var(--bg-card); padding: 40px; border-radius: 20px; box-shadow: 0 20px 60px rgba(0,0,0,0.3); width: 100%; max-width: 420px; animation: slideUp 0.5s ease-out; }
        @keyframes slideUp { from { opacity: 0; transform: translateY(30px); } to { opacity: 1; transform: translateY(0); } }
        .login-header { text-align: center; margin-bottom: 30px; }
        .login-header h1 { font-size: 2em; margin-bottom: 8px; background: linear-gradient(135deg, #6366f1, #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .form-group { margin-bottom: 20px; }
        .form-group label { display: block; margin-bottom: 8px; font-weight: 500; font-size: 0.9em; }
        .form-input { width: 100%; padding: 12px 16px; border: 2px solid var(--border); border-radius: 10px; font-size: 1em; transition: all 0.3s; background: var(--bg-secondary); color: var(--text-primary); }
        .form-input:focus { outline: none; border-color: var(--accent); }
        .btn { width: 100%; padding: 14px; border: none; border-radius: 10px; font-size: 1em; font-weight: 600; cursor: pointer; transition: all 0.3s; }
        .btn-primary { background: linear-gradient(135deg, var(--accent), #8b5cf6); color: white; }
        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 20px rgba(99, 102, 241, 0.4); }
        .toggle-form { text-align: center; margin-top: 20px; color: var(--text-secondary); }
        .toggle-form a { color: var(--accent); cursor: pointer; font-weight: 600; }
        .alert { padding: 12px; border-radius: 8px; margin-bottom: 16px; font-size: 0.9em; }
        .alert-error { background: rgba(239, 68, 68, 0.1); color: var(--danger); border: 1px solid var(--danger); }
        .alert-success { background: rgba(16, 185, 129, 0.1); color: var(--success); border: 1px solid var(--success); }

        /* ========== APP ========== */
        .app-container { display: none; }
        .app-container.active { display: block; }
        .navbar { background: var(--bg-card); border-bottom: 1px solid var(--border); padding: 16px 0; position: sticky; top: 0; z-index: 100; box-shadow: 0 2px 10px var(--shadow); }
        .navbar-content { max-width: 1400px; margin: 0 auto; padding: 0 20px; display: flex; justify-content: space-between; align-items: center; }
        .logo { font-size: 1.5em; font-weight: 700; background: linear-gradient(135deg, var(--accent), #8b5cf6); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
        .navbar-actions { display: flex; gap: 12px; align-items: center; }
        .theme-toggle { background: var(--bg-secondary); border: none; width: 40px; height: 40px; border-radius: 10px; cursor: pointer; font-size: 1.2em; transition: all 0.3s; }
        .user-info { display: flex; align-items: center; gap: 12px; padding: 8px 16px; background: var(--bg-secondary); border-radius: 10px; }
        .user-avatar { width: 32px; height: 32px; background: linear-gradient(135deg, var(--accent), #8b5cf6); border-radius: 50%; display: flex; align-items: center; justify-content: center; color: white; font-weight: 600; }
        .btn-logout { padding: 8px 16px; background: var(--danger); color: white; border: none; border-radius: 8px; cursor: pointer; font-weight: 500; transition: all 0.3s; }
        .tabs { max-width: 1400px; margin: 0 auto; padding: 20px 20px 0; display: flex; gap: 8px; border-bottom: 2px solid var(--border); overflow-x: auto; }
        .tab { padding: 12px 24px; background: transparent; border: none; border-bottom: 3px solid transparent; cursor: pointer; font-size: 1em; font-weight: 600; color: var(--text-secondary); transition: all 0.3s; white-space: nowrap; }
        .tab.active { color: var(--accent); border-bottom-color: var(--accent); }
        .main-content { max-width: 1400px; margin: 0 auto; padding: 30px 20px; }
        .tab-content { display: none; animation: fadeIn 0.3s; }
        .tab-content.active { display: block; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }

        /* ========== UI COMPONENTS ========== */
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: var(--bg-card); padding: 24px; border-radius: 16px; border: 1px solid var(--border); box-shadow: 0 2px 8px var(--shadow); transition: all 0.3s; }
        .stat-icon { font-size: 1.5em; margin-bottom: 8px; }
        .stat-label { font-size: 0.85em; color: var(--text-secondary); margin-bottom: 8px; }
        .stat-value { font-size: 1.8em; font-weight: 700; }
        .section-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; flex-wrap: wrap; gap: 15px; }
        .section-title { font-size: 1.5em; font-weight: 700; }
        .btn-add { padding: 12px 24px; background: var(--accent); color: white; border: none; border-radius: 10px; cursor: pointer; font-weight: 600; transition: all 0.3s; display: flex; align-items: center; gap: 8px; }
        .tasks-container { display: flex; flex-direction: column; gap: 12px; }
        .task-card { background: var(--bg-card); padding: 20px; border-radius: 12px; border: 2px solid var(--border); box-shadow: 0 2px 8px var(--shadow); transition: all 0.3s; cursor: pointer; }
        .task-card.completed { border-color: var(--success); opacity: 0.8; }
        .task-card.overdue { border-color: var(--danger); }
        .task-header { display: flex; justify-content: space-between; margin-bottom: 12px; }
        .task-title { font-size: 1.1em; font-weight: 600; }
        .task-status { display: inline-block; padding: 4px 10px; border-radius: 12px; font-size: 0.75em; font-weight: 600; margin-top: 8px; }
        .spaced-badge { background: #8b5cf6; color: white; padding: 2px 6px; border-radius: 6px; font-size: 0.7em; margin-left: 10px; vertical-align: middle; }

        /* ========== ABA IA & SEGUNDO CÉREBRO ========== */
        .ai-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 20px; }
        @media(max-width: 900px) { .ai-grid { grid-template-columns: 1fr; } }
        .graph-container { border: 2px solid var(--border); border-radius: 16px; height: 500px; background: var(--bg-secondary); overflow: hidden; }
        
        .chat-container { border: 2px solid var(--border); border-radius: 16px; display: flex; flex-direction: column; height: 500px; background: var(--bg-card); }
        .chat-messages { flex: 1; padding: 20px; overflow-y: auto; display: flex; flex-direction: column; gap: 10px; }
        .chat-bubble { max-width: 80%; padding: 12px 16px; border-radius: 12px; font-size: 0.95em; }
        .chat-ai { background: rgba(99, 102, 241, 0.1); border-bottom-left-radius: 2px; align-self: flex-start; }
        .chat-user { background: var(--accent); color: white; border-bottom-right-radius: 2px; align-self: flex-end; }
        .chat-input { display: flex; padding: 15px; border-top: 1px solid var(--border); gap: 10px; }
        
        .ai-tools { display: grid; grid-template-columns: repeat(auto-fit, minmax(200px, 1fr)); gap: 15px; margin-top: 20px; }
        .ai-tool-btn { padding: 20px; text-align: center; border: 2px dashed var(--accent); border-radius: 12px; cursor: pointer; color: var(--text-primary); font-weight: 600; transition: 0.3s; background: transparent; }
        .ai-tool-btn:hover { background: rgba(99, 102, 241, 0.05); transform: translateY(-2px); }

        /* ========== ABA FOCO & PROVA ========== */
        .focus-panel { background: var(--bg-card); border: 2px solid var(--border); border-radius: 20px; padding: 40px; text-align: center; }
        .timer-display { font-size: 6rem; font-weight: bold; font-variant-numeric: tabular-nums; color: var(--accent); margin: 20px 0; }
        .focus-controls { display: flex; justify-content: center; gap: 15px; margin-bottom: 30px; flex-wrap: wrap; }
        .focus-select { padding: 12px; border-radius: 10px; border: 2px solid var(--border); background: var(--bg-secondary); color: var(--text-primary); font-size: 1em; }
        .btn-exam { background: var(--danger); color: white; width: auto; padding: 15px 40px; }
        
        /* OVERLAY PROVA SIMULADA */
        #examOverlay { display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: #000000; z-index: 9999; color: white; flex-direction: column; align-items: center; justify-content: center; }
        #examOverlay.active { display: flex; }
        .exam-timer { font-size: 8rem; color: var(--danger); font-weight: bold; margin-bottom: 30px; }

        /* ========== MODAL ========== */
        .modal { display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.6); z-index: 1000; align-items: center; justify-content: center; padding: 20px; backdrop-filter: blur(4px); }
        .modal.active { display: flex; }
        .modal-content { background: var(--bg-card); padding: 30px; border-radius: 16px; width: 100%; max-width: 600px; max-height: 90vh; overflow-y: auto; }
        .btn-close { background: none; border: none; font-size: 1.5em; cursor: pointer; color: var(--text-secondary); }
    </style>
</head>
<body>

    <!-- PAINEL DO RESPONSÁVEL -->
    <div class="parent-dashboard">
        <strong>👁️ Painel do Responsável</strong> | Monitorando: 
        <select id="childSelector" style="padding: 5px; border-radius: 5px; margin: 0 10px;">
            <option>Selecione um filho...</option>
            <option value="filho1">Joãozinho Silva</option>
        </select>
        <button onclick="sendParentMessage()" style="padding: 5px 15px; border-radius: 5px; border: none; background: white; color: var(--warning); font-weight: bold; cursor: pointer;">📩 Enviar Cobrança</button>
    </div>

    <!-- LOGIN PAGE -->
    <div class="login-container" id="authPage">
        <div class="login-box">
            <div class="login-header">
                <h1>📚 StudyHub 2.0</h1>
                <p id="authSubtitle">Plataforma de Alta Performance</p>
            </div>

            <div id="alertContainer"></div>

            <form id="loginForm">
                <div class="form-group">
                    <label>E-mail</label>
                    <input type="email" id="loginEmail" class="form-input" required>
                </div>
                <div class="form-group">
                    <label>Senha</label>
                    <input type="password" id="loginPassword" class="form-input" required>
                </div>
                <button type="submit" class="btn btn-primary">🔐 Entrar</button>
            </form>

            <form id="registerForm" style="display: none;">
                <div class="form-group">
                    <label>Eu sou:</label>
                    <select id="regRole" class="form-input" onchange="toggleRegFields()" required>
                        <option value="student">Aluno</option>
                        <option value="parent">Pai / Responsável</option>
                    </select>
                </div>
                <div class="form-group">
                    <label>Nome completo</label>
                    <input type="text" id="registerName" class="form-input" required>
                </div>
                <div class="form-group">
                    <label>E-mail</label>
                    <input type="email" id="registerEmail" class="form-input" required>
                </div>
                
                <!-- Campos Dinâmicos WhatsApp -->
                <div id="whatsappFields">
                    <div class="form-group">
                        <label>WhatsApp do Responsável (Obrigatório para Alertas)</label>
                        <input type="text" id="regParentPhone" class="form-input" placeholder="(00) 00000-0000">
                    </div>
                    <div class="form-group">
                        <label>Seu WhatsApp (Opcional)</label>
                        <input type="text" id="regStudentPhone" class="form-input" placeholder="(00) 00000-0000">
                    </div>
                </div>

                <div class="form-group">
                    <label>Senha</label>
                    <input type="password" id="registerPassword" class="form-input" required minlength="6">
                </div>
                <button type="submit" class="btn btn-primary">✨ Criar conta</button>
            </form>

            <div class="toggle-form">
                <span id="toggleText">Não tem uma conta?</span>
                <a id="toggleLink">Criar conta</a>
            </div>
        </div>
    </div>

    <!-- APP -->
    <div class="app-container" id="appContainer">
        <nav class="navbar">
            <div class="navbar-content">
                <div class="logo">📚 StudyHub 2.0</div>
                <div class="navbar-actions">
                    <button class="theme-toggle" id="themeToggle">🌙</button>
                    <div class="user-info">
                        <div class="user-avatar" id="userAvatar">A</div>
                        <span id="userName">Usuário</span>
                    </div>
                    <button class="btn-logout" id="logoutBtn">Sair</button>
                </div>
            </div>
        </nav>

        <div class="tabs">
            <button class="tab active" data-tab="tasks">📋 Tarefas</button>
            <button class="tab" data-tab="ai">🧠 Inteligência (IA)</button>
            <button class="tab" data-tab="focus">⏱️ Foco & Prova</button>
            <button class="tab" data-tab="exams">📝 Provas</button>
            <button class="tab" data-tab="planning">📅 Planejamento</button>
        </div>

        <main class="main-content">
            
            <!-- TAREFAS TAB -->
            <div class="tab-content active" id="tasks-tab">
                
                <!-- Caixa de Entrada do Aluno -->
                <div class="student-inbox" id="studentInbox">
                    <strong>🔔 Mensagem do seu Responsável:</strong>
                    <span id="inboxMessage" style="margin-left: 10px;">Nenhuma mensagem nova.</span>
                </div>

                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-icon">📋</div>
                        <div class="stat-label">Total de Tarefas</div>
                        <div class="stat-value" id="statTotal">0</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">⏳</div>
                        <div class="stat-label">Pendentes</div>
                        <div class="stat-value" id="statPending">0</div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-icon">✅</div>
                        <div class="stat-label">Concluídas</div>
                        <div class="stat-value" id="statCompleted">0</div>
                    </div>
                </div>

                <div class="section-header">
                    <h2 class="section-title">Minhas Tarefas</h2>
                    <button class="btn-add hide-on-parent" id="addTaskBtn">➕ Nova Tarefa</button>
                </div>

                <div class="tasks-container" id="tasksContainer"></div>
            </div>

            <!-- NOVA ABA: INTELIGÊNCIA (IA) -->
            <div class="tab-content" id="ai-tab">
                <div class="section-header">
                    <h2 class="section-title">Segundo Cérebro & Ferramentas IA</h2>
                </div>

                <div class="ai-grid">
                    <!-- Grafo de Conhecimento -->
                    <div>
                        <h3 style="margin-bottom: 15px;">🌐 Seu Grafo de Conhecimento</h3>
                        <div id="knowledgeGraph" class="graph-container"></div>
                        <small style="color: var(--text-secondary); display: block; margin-top: 10px;">Conexões geradas automaticamente com base nas suas tarefas e estudos.</small>
                    </div>

                    <!-- Inquisidor Socrático -->
                    <div class="chat-container">
                        <div style="padding: 15px; border-bottom: 1px solid var(--border); font-weight: bold;">
                            🤺 Inquisidor Socrático (Técnica Feynman)
                        </div>
                        <div class="chat-messages" id="chatBox">
                            <div class="chat-bubble chat-ai">
                                Olá. Eu não dou respostas. Eu testo o seu conhecimento. Sobre qual matéria você quer provar que tem domínio hoje?
                            </div>
                        </div>
                        <div class="chat-input">
                            <input type="text" id="chatInput" class="form-input" placeholder="Ex: Quero testar Mitose...">
                            <button class="btn btn-primary" style="width: auto;" onclick="sendChatMessage()">Enviar</button>
                        </div>
                    </div>
                </div>

                <!-- Ferramentas de IA -->
                <div class="ai-tools">
                    <button class="ai-tool-btn" onclick="alert('Em breve: Envie a foto e extrairemos o texto!')">
                        📸 Scanner de Lousa e Livros (OCR)
                    </button>
                    <button class="ai-tool-btn" onclick="alert('Em breve: Cole seu texto e daremos nota ENEM!')">
                        ✍️ Corretor de Redação IA
                    </button>
                    <button class="ai-tool-btn" onclick="alert('Em breve: Transformaremos seu resumo em Cartões!')">
                        🃏 Gerar Flashcards Inteligentes
                    </button>
                </div>
            </div>

            <!-- NOVA ABA: FOCO & PROVA SIMULADA -->
            <div class="tab-content" id="focus-tab">
                <div class="focus-panel">
                    <h2 style="font-size: 2em; margin-bottom: 10px;">Modo Foco Personalizado</h2>
                    <p style="color: var(--text-secondary); margin-bottom: 30px;">Livre de distrações. Configure seu ambiente de estudo.</p>
                    
                    <div class="focus-controls">
                        <select id="focusMode" class="focus-select">
                            <option value="pomodoro">🍅 Pomodoro (25 min)</option>
                            <option value="stopwatch">⏱️ Estudo Direto (Contínuo)</option>
                        </select>
                        <select id="focusSound" class="focus-select">
                            <option value="none">🔇 Sem Som</option>
                            <option value="rain">🌧️ Chuva</option>
                            <option value="lofi">🎧 Música Lo-Fi</option>
                            <option value="jazz">🎷 Jazz Suave</option>
                        </select>
                    </div>

                    <div class="timer-display" id="timerDisplay">25:00</div>
                    
                    <div style="display: flex; justify-content: center; gap: 15px;">
                        <button class="btn btn-primary" id="btnStartTimer" style="width: 200px;">▶️ Começar</button>
                        <button class="btn" id="btnStopTimer" style="width: 200px; background: var(--bg-secondary); color: var(--text-primary);">⏹️ Parar</button>
                    </div>

                    <hr style="border: none; border-top: 1px solid var(--border); margin: 40px 0;">

                    <h3 style="color: var(--danger); margin-bottom: 15px;">⚠️ Treinamento de Alta Pressão</h3>
                    <button class="btn btn-exam" onclick="startExamMode()">🔒 Iniciar Prova Simulada (Tela Travada)</button>
                </div>
            </div>

            <!-- PROVAS E PLANEJAMENTO (Oculto código repetido para simplificar, mas mantidos os IDs originais) -->
            <div class="tab-content" id="exams-tab">
                <div class="section-header">
                    <h2 class="section-title">Minhas Provas</h2>
                    <button class="btn-add hide-on-parent" id="addExamBtn">➕ Nova Prova</button>
                </div>
                <div class="tasks-container" id="examsContainer"></div>
            </div>

            <div class="tab-content" id="planning-tab">
                <div class="section-header">
                    <h2 class="section-title">Planejamento</h2>
                </div>
                <div id="planningContainer"></div>
            </div>
        </main>
    </div>

    <!-- OVERLAY PROVA SIMULADA -->
    <div id="examOverlay">
        <h2>Modo Prova Simulada Ativo</h2>
        <p>A tela está travada para evitar distrações. Foco total!</p>
        <div class="exam-timer" id="examOverlayTimer">45:00</div>
        <button class="btn" style="background: white; color: black; width: 200px;" onclick="stopExamMode()">Finalizar Prova</button>
    </div>

    <!-- MODAL TAREFA ATUALIZADO (REVISÃO ESPAÇADA) -->
    <div class="modal" id="taskModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title" id="taskModalTitle">Nova Tarefa</h2>
                <button class="btn-close" onclick="closeModal('taskModal')">×</button>
            </div>
            <form id="taskForm">
                <div class="form-group">
                    <label>Matéria *</label>
                    <input type="text" id="taskSubject" class="form-input" required>
                </div>
                <div class="form-group">
                    <label>Tarefa *</label>
                    <textarea id="taskDescription" class="form-input" required></textarea>
                </div>
                <div class="form-group">
                    <label>Data de entrega *</label>
                    <input type="date" id="taskDeadline" class="form-input" required>
                </div>
                <div class="form-group" style="display: flex; gap: 10px; align-items: center; background: rgba(139, 92, 246, 0.1); padding: 15px; border-radius: 8px;">
                    <input type="checkbox" id="taskSpaced" style="width: 18px; height: 18px;">
                    <label style="margin: 0;">Ativar Revisão Espaçada (1, 3 e 7 dias)</label>
                </div>
                <button type="submit" class="btn btn-primary" style="margin-top: 15px;">💾 Salvar</button>
            </form>
        </div>
    </div>

    <!-- MODAL DETALHE -->
    <div class="modal" id="detailModal">
        <div class="modal-content">
            <div class="modal-header">
                <h2 class="modal-title" id="detailTitle">Detalhes</h2>
                <button class="btn-close" onclick="closeModal('detailModal')">×</button>
            </div>
            <div id="detailContent"></div>
        </div>
    </div>

    <script>
        let currentUser = null;
        let tasks = [];
        let exams = [];

        // ========== SISTEMA DE LOGIN E CONTAS (PAI / ALUNO) ==========
        function toggleRegFields() {
            const role = document.getElementById('regRole').value;
            const fields = document.getElementById('whatsappFields');
            const parentPhone = document.getElementById('regParentPhone');
            
            if (role === 'student') {
                fields.style.display = 'block';
                parentPhone.setAttribute('required', 'true');
            } else {
                fields.style.display = 'none';
                parentPhone.removeAttribute('required');
            }
        }

        document.getElementById('registerForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const user = {
                id: Date.now().toString(),
                role: document.getElementById('regRole').value,
                name: document.getElementById('registerName').value,
                email: document.getElementById('registerEmail').value
            };
            localStorage.setItem('studyhub_users', JSON.stringify([user]));
            alert('Conta criada! Faça o login.');
        });

        document.getElementById('loginForm').addEventListener('submit', (e) => {
            e.preventDefault();
            // Simulação de Login Rápido (Qualquer dado entra para demonstração)
            const role = document.getElementById('loginEmail').value.includes('pai') ? 'parent' : 'student';
            currentUser = { id: '1', role: role, name: role === 'parent' ? 'Responsável' : 'Estudante' };
            showApp();
        });

        function showApp() {
            document.getElementById('authPage').style.display = 'none';
            document.getElementById('appContainer').classList.add('active');
            document.getElementById('userName').textContent = currentUser.name;
            
            // Aplica restrições baseadas no papel
            document.body.className = ''; 
            if (currentUser.role === 'parent') {
                document.body.classList.add('is-parent');
                alert('Modo Responsável Ativo: Você só pode visualizar e enviar mensagens.');
            } else {
                document.body.classList.add('is-student');
                checkParentMessages();
            }

            // Simula algumas tarefas pré-existentes
            if (tasks.length === 0) {
                tasks = [
                    { id: '1', subject: 'Física', description: 'Leis de Newton', deadline: new Date().toISOString().split('T')[0], completed: false, spaced: true },
                    { id: '2', subject: 'Matemática', description: 'Equação 2º Grau', deadline: new Date().toISOString().split('T')[0], completed: true, spaced: false }
                ];
            }
            renderTasks();
            initKnowledgeGraph();
        }

        // ========== COMUNICAÇÃO PAI E FILHO ==========
        function sendParentMessage() {
            const msg = prompt("Digite a mensagem para enviar ao painel do seu filho:");
            if (msg) {
                localStorage.setItem('parentMessage', msg);
                alert("Mensagem enviada com sucesso! O sistema também dispararia um WhatsApp agora.");
            }
        }

        function checkParentMessages() {
            const msg = localStorage.getItem('parentMessage');
            if (msg) document.getElementById('inboxMessage').innerHTML = `<strong>${msg}</strong>`;
        }

        // ========== TABS ==========
        document.querySelectorAll('.tab').forEach(tab => {
            tab.addEventListener('click', () => {
                document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
                document.querySelectorAll('.tab-content').forEach(c => c.classList.remove('active'));
                tab.classList.add('active');
                document.getElementById(`${tab.dataset.tab}-tab`).classList.add('active');
            });
        });

        // ========== TAREFAS E REVISÃO ESPAÇADA ==========
        document.getElementById('addTaskBtn').addEventListener('click', () => {
            document.getElementById('taskForm').reset();
            document.getElementById('taskModal').classList.add('active');
        });

        document.getElementById('taskForm').addEventListener('submit', (e) => {
            e.preventDefault();
            const spaced = document.getElementById('taskSpaced').checked;
            const task = {
                id: Date.now().toString(),
                subject: document.getElementById('taskSubject').value,
                description: document.getElementById('taskDescription').value,
                deadline: document.getElementById('taskDeadline').value,
                completed: false,
                spaced: spaced
            };
            tasks.push(task);
            
            // Simula Criação da Revisão Espaçada
            if(spaced) {
                alert("Sistema Agendou Revisões Ocultas para 1, 3 e 7 dias automaticamente!");
                // Atualiza o Grafo de Conhecimento
                updateGraph(task.subject);
            }

            closeModal('taskModal');
            renderTasks();
        });

        function renderTasks() {
            const container = document.getElementById('tasksContainer');
            container.innerHTML = tasks.map(t => `
                <div class="task-card ${t.completed ? 'completed' : ''}" onclick="showDetail('${t.id}')">
                    <div class="task-header">
                        <div>📚 ${t.subject} ${t.spaced ? '<span class="spaced-badge">🔄 Revisão Espaçada Ativa</span>' : ''}</div>
                    </div>
                    <div>${t.description}</div>
                </div>
            `).join('');
        }

        function showDetail(id) {
            const t = tasks.find(x => x.id === id);
            document.getElementById('detailTitle').textContent = t.subject;
            document.getElementById('detailContent').innerHTML = `
                <p>${t.description}</p>
                <div class="btn-group hide-on-parent">
                    <button class="btn-secondary" onclick="toggleComplete('${id}')">${t.completed ? 'Reabrir' : '✅ Concluir'}</button>
                    <button class="btn-danger" onclick="deleteTask('${id}')">🗑️ Excluir</button>
                </div>
            `;
            document.getElementById('detailModal').classList.add('active');
        }

        function toggleComplete(id) {
            const t = tasks.find(x => x.id === id);
            t.completed = !t.completed;
            closeModal('detailModal');
            renderTasks();
        }

        function deleteTask(id) {
            tasks = tasks.filter(x => x.id !== id);
            closeModal('detailModal');
            renderTasks();
        }

        function closeModal(id) { document.getElementById(id).classList.remove('active'); }

        // ========== GRAFO DE CONHECIMENTO (SEGUNDO CÉREBRO) ==========
        let network;
        let graphNodes = new vis.DataSet([
            { id: 1, label: 'Matemática', color: '#6366f1' },
            { id: 2, label: 'Física', color: '#10b981' }
        ]);
        let graphEdges = new vis.DataSet([ { from: 1, to: 2 } ]);

        function initKnowledgeGraph() {
            const container = document.getElementById('knowledgeGraph');
            const data = { nodes: graphNodes, edges: graphEdges };
            const options = { nodes: { shape: 'dot', size: 20, font: { color: 'var(--text-primary)' } }, physics: { stabilization: true } };
            network = new vis.Network(container, data, options);
        }

        function updateGraph(newSubject) {
            // Verifica se não existe e adiciona no mapa 3D
            if(!graphNodes.get({filter: function(item) { return item.label === newSubject; }}).length) {
                const newId = graphNodes.length + 1;
                graphNodes.add({ id: newId, label: newSubject, color: '#f59e0b' });
                // Conecta aleatoriamente a algo existente (só para visualização)
                graphEdges.add({ from: newId, to: 1 });
            }
        }

        // ========== INQUISIDOR SOCRÁTICO (CHAT) ==========
        function sendChatMessage() {
            const input = document.getElementById('chatInput');
            const box = document.getElementById('chatBox');
            if(!input.value) return;

            box.innerHTML += `<div class="chat-bubble chat-user">${input.value}</div>`;
            const userText = input.value;
            input.value = '';
            
            // Simula resposta da IA Socrática
            setTimeout(() => {
                box.innerHTML += `<div class="chat-bubble chat-ai">Interessante sua visão sobre "${userText}". Mas se isso é verdade, como você explicaria a falha nessa teoria usando palavras que uma criança de 10 anos entenderia?</div>`;
                box.scrollTop = box.scrollHeight;
            }, 1000);
        }

        // ========== MODO FOCO E PROVA SIMULADA ==========
        let timerInterval;
        let timeLeft = 25 * 60; // 25 min
        let isRunning = false;

        document.getElementById('btnStartTimer').addEventListener('click', () => {
            if(isRunning) return;
            isRunning = true;
            
            // Simula Tocar Áudio
            const sound = document.getElementById('focusSound').value;
            if(sound !== 'none') alert(`🔊 Reproduzindo Som Ambiente: ${sound.toUpperCase()}`);

            timerInterval = setInterval(() => {
                if(timeLeft <= 0) { clearInterval(timerInterval); alert("Tempo Esgotado!"); isRunning = false; return; }
                timeLeft--;
                const m = Math.floor(timeLeft / 60).toString().padStart(2, '0');
                const s = (timeLeft % 60).toString().padStart(2, '0');
                document.getElementById('timerDisplay').textContent = `${m}:${s}`;
            }, 1000);
        });

        document.getElementById('btnStopTimer').addEventListener('click', () => {
            clearInterval(timerInterval);
            isRunning = false;
        });

        // TELA CHEIA (PROVA SIMULADA)
        function startExamMode() {
            const elem = document.documentElement;
            if (elem.requestFullscreen) elem.requestFullscreen();
            document.getElementById('examOverlay').classList.add('active');
            
            // Cronômetro da Prova
            let examTime = 45 * 60;
            setInterval(() => {
                examTime--;
                const m = Math.floor(examTime / 60).toString().padStart(2, '0');
                const s = (examTime % 60).toString().padStart(2, '0');
                document.getElementById('examOverlayTimer').textContent = `${m}:${s}`;
            }, 1000);
        }

        function stopExamMode() {
            if (document.exitFullscreen) document.exitFullscreen();
            document.getElementById('examOverlay').classList.remove('active');
        }
    </script>
</body>
</html>